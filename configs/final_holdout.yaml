# Final Frozen Holdout Benchmark Configuration
version: "1.0.0"
experiment_name: "final_frozen_holdout_benchmark"
dataset_size: 20000
random_seed: 999888777 # Independent seed isolated from training/validation
scenario: "S1_HIGH_NATURAL_RECOVERY"

attribution_window_hours: 72
bootstrap_iterations: 2000
bootstrap_seed: 1337
confidence_level: 0.95

paths:
  contract_config: "configs/experiment_contract.yaml"
  costs_config: "configs/costs.yaml"
  policy_config: "configs/policy.yaml"
  output_dir: "evaluation_results/final_holdout"
